name: riff_switch
description: This is a package for a custom Flutter Switch widget called RiffSwitch.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.0.0 <4.0.0'

  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

# The following section is specific to Flutter packages.
flutter:

